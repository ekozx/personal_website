extends layout

block content
  .post
    - myImg = '/images/' + post.picture
    a.articleImg.single-img(href='/single/' + post.indexTitle)
      img(src=myImg)
    h1.post-title= post.title
    blockquote
      .preview=post.preview
    .post-body!= parsedBody
