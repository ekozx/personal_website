extends layout

block content
  each post in posts
    article.post
      - var stamp = new Date(String(post.created_at))
      p= Number(stamp.getMonth()) + 1
      h4= post.title
      p= post.preview
