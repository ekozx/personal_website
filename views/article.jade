article.post
  - var stamp = new Date(String(post.created_at))
  .circle-date
    .date-text= (Number(stamp.getMonth()) + 1) + "/" + String(stamp.getFullYear()).substring(2, 4)
  - myImg = '/images/' + post.picture
  img.articleImg(src=myImg)
  h4= post.title
  p= post.preview
